VPN Leak Detection System
Features:
    Real-time DNS leak detection
    IP address exposure monitoring
    WebRTC leak prevention
    Multi-layer analysis engine
    Live dashboard with visual alerts
    Browser extension support


Backend: cd backend && pip install -r requirements.txt && python app.py
Frontend: Open frontend/dashboard.html in browser
Extension: Load browser-extension/ as unpacked extension

